import React from 'react';
import { Heart, Search, Bell } from 'lucide-react';
import { feedCategories } from '../../data/feedData';

export default function FeedPageMobile({ filteredOutfits, activeCategory, setActiveCategory, likedOutfits, toggleLike, onSelectOutfit, onNavigateToSearch, onOpenAuth }) {
  return <div className="bg-[#FBFBFA] min-h-screen text-[#151816] pb-10"><header className="px-5 pt-4 pb-3 flex items-center justify-between border-b border-[#EAE6DF] sticky top-0 bg-[#FBFBFA]/95 z-30"><h1 className="font-serif-luxury text-2xl font-bold tracking-wider text-[#183B22]">DRAPE</h1><div className="flex items-center space-x-3"><button onClick={onNavigateToSearch}><Search className="w-5 h-5" /></button><button onClick={onOpenAuth}><Bell className="w-5 h-5" /></button></div></header><div className="px-5 py-3 flex gap-2 overflow-x-auto no-scrollbar">{feedCategories.slice(0, 5).map((cat) => <button key={cat.id} onClick={() => setActiveCategory(cat.id)} className={`px-4 py-1.5 text-xs rounded-full whitespace-nowrap ${activeCategory === cat.id ? 'bg-[#183B22] text-white' : 'bg-white border border-[#E3DED5]'}`}>{cat.label}</button>)}</div><div className="px-4 pt-2 grid grid-cols-2 gap-3.5">{filteredOutfits.map((outfit) => { const isLiked = likedOutfits.includes(outfit.id); return <div key={outfit.id} onClick={() => onSelectOutfit(outfit)} className="relative rounded-2xl overflow-hidden cursor-pointer bg-[#F4F0E8]" style={{ minHeight: outfit.aspect === 'tall' ? '260px' : outfit.aspect === 'wide' ? '170px' : '210px' }}><img src={outfit.image} alt={outfit.title} className="absolute inset-0 w-full h-full object-cover" /><button onClick={(e) => toggleLike(outfit.id, e)} className="absolute top-2.5 right-2.5 z-10 w-7 h-7 rounded-full bg-white/90 flex items-center justify-center"><Heart className={`w-4 h-4 ${isLiked ? 'fill-[#C53030] text-[#C53030]' : ''}`} /></button><div className="absolute bottom-2.5 left-2.5 right-2.5 p-2 rounded-xl bg-white/85"><h4 className="font-serif-luxury text-xs font-bold truncate">{outfit.title}</h4><div className="text-[10px] text-[#1C4A29] font-semibold">{outfit.matchBadge}</div></div></div>; })}</div></div>;
}
